# Disable USB autosuspend on the Bluetooth radio.
#
# The Intel BE200 controller defaults to btusb enable_autosuspend=Y, which lets
# USB power management idle the radio and causes random A2DP audio dropouts
# (e.g. AirPods cutting in and out during playback). Keeping it powered fixes it.
options btusb enable_autosuspend=0
