# noctalia-greeter greeter.conf
# default_session: admin default (Wayland session Name=)
# default_user: skip user picker, open password for this account
# session: last used (UI); scheme: color scheme name
# output: Wayland connector; output_layout: monitor positions; admin-only
# scale: UI scale; cursor_theme/cursor_size/cursor_path: cursor appearance
# keyboard_layout/keyboard_variant/keyboard_options: XKB keymap (compositor)
greeter_user=lorenzo
default_session="Hyprland (uwsm-managed)"
default_user=lorenzo
session="Hyprland (uwsm-managed)"
scheme=Synced
cursor_theme=catppuccin-mocha-lavender-cursors
cursor_size=24
