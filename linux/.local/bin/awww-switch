#!/usr/bin/env bash

WALLPAPER_DIR="$HOME/Pictures/Wallpapers"
RANDOM_WALLPAPER=$(find "$WALLPAPER_DIR" -type f | shuf -n 1)

if [ -n "$RANDOM_WALLPAPER" ]; then
  awww img -t wave "$RANDOM_WALLPAPER"
fi
